<%= form_for(@distintivo) do |f| %>
  <% if @distintivo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@distintivo.errors.count, "error") %> prohibited this distintivo from being saved:</h2>

      <ul>
      <% @distintivo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %><br>
    <%= f.text_field :nome %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
