<%= form_for(@cerimonia) do |f| %>
  <% if @cerimonia.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cerimonia.errors.count, "error") %> prohibited this cerimonia from being saved:</h2>

      <ul>
      <% @cerimonia.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :socio_id %><br>
    <%= f.text_field :socio_id %>
  </div>
  <div class="field">
    <%= f.label :distintivo_id %><br>
    <%= f.text_field :distintivo_id %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.date_select :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
